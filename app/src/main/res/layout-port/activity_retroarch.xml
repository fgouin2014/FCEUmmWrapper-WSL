<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

                        <!-- **TEST** : EmulatorView plein écran avec background transparent -->
                    <com.fceumm.wrapper.EmulatorView
                        android:id="@+id/emulator_view"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:layout_margin="0dp"
                        android:padding="0dp"
                        android:background="@android:color/transparent" />

                        <!-- **CORRECTION** : Zone de contrôles supprimée pour mode portrait -->
                    <!-- La zone de contrôles n'est pas nécessaire en mode portrait -->
                    <!-- L'overlay couvre tout l'écran -->

                        <!-- **CORRECTION** : Overlay restauré pour les boutons -->
                    <com.fceumm.wrapper.overlay.OverlayRenderView
                        android:id="@+id/overlay_render_view"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:layout_margin="0dp"
                        android:padding="0dp"
                        android:background="@android:color/transparent"
                        android:visibility="visible" />

</FrameLayout> 